# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg-info/
.eggs/
build/
dist/

# Virtual environments
.venv/
venv/
env/

# uv
.uv/

# Type checkers / linters
.mypy_cache/
.ruff_cache/
.pytest_cache/
.coverage
.coverage.*
htmlcov/
coverage.xml

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Claude Code (per-machine settings stay local)
.claude/settings.local.json
.claude/.credentials.json
# Local-only devops notes and private context (never reaches public repo)
.claude/local/

# Home Assistant dev artefacts
.HA_VERSION
config/

# Worktrees live as siblings (../gaggle.wt/), not nested,
# but ignore the dir defensively in case someone tries the nested layout.
.worktrees/

# Local secrets (defence-in-depth; gitleaks also scans)
.env
.env.*
!.env.example
security/
.venv

# Proprietary conformance-review material (workstation-only — the public
# record is docs/compliance/secure-sdlc-standard.md + conformance.md)
docs/compliance/sdlc-standard-review-*.md
docs/compliance/sdlc-review-*-workpapers/
docs/compliance/remediation-program-*.md
docs/compliance/remediation-*-workpapers/
