# Gaggle

**Unofficial community integration.** Not affiliated with, endorsed by, or
supported by AGL Energy Limited. The API contract used by this integration is
not publicly documented and may change at any time.

**Australia only.** Requires an AGL Energy gas account.

Pulls smart-meter gas usage from AGL Australia and feeds it into the Home
Assistant Energy dashboard using AGL's authenticated mobile API.

> **Status: pre-release, functional against a basic (non-smart) gas
> meter.** A basic gas meter has no interval/daily data — AGL only exposes
> a current-period estimate plus a window of already-billed past periods,
> so the Energy dashboard gets one bar per completed billing period
> (roughly bimonthly), not a daily chart. No release has shipped yet.

## Setup

1. Install **Gaggle** from HACS, then restart Home Assistant.
2. *Settings* → *Devices & Services* → *Add integration* → search **AGL Gaggle**.
3. A login URL is displayed. Open it in your **real browser** (not HA's built-in
   browser) and complete your AGL login including any MFA prompt.
4. After login AGL redirects to a "Not Found" page. Copy the full URL from your
   browser's address bar and paste it back into the HA dialog.
5. If you have multiple gas contracts, select the one to monitor.

## Energy dashboard

Add the **`gaggle:consumption_<contract>`** statistic as your dashboard's
*Gas consumption* source — never the `sensor.…` entities (they update once
per poll and would chart a whole day as one bar on the wrong date).

Full guide:
[docs/energy-dashboard.md](https://github.com/teknikqa/gaggle/blob/main/docs/energy-dashboard.md)

## Sensors

- **Consumption / Consumption this period** — cumulative total and current
  bill-period usage (the current period is AGL's own estimate).
- **Consumption cost / Bill projection** — AUD so far this period, and
  AGL's own forecast for the full bill.
- **Unit rate / Supply charge** — your tariff and daily supply charge.
  Gas plans are typically tiered; the unit rate sensor reads the highest
  ("Thereafter") tier.

## Provenance

This integration is AI-generated by Claude Code and reviewed by a human.
Ported from [`haggle`](https://github.com/NaanyaBiz/haggle) (AGL
electricity). See the repo's `AGENTS.md` for the dev workflow.
